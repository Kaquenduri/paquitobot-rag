# openspec/changes/archive/

Audit trail of completed Primer RAG SDD changes. Each archived change lives in
`openspec/changes/archive/YYYY-MM-DD-<change-name>/` (ISO date prefix).

The archive is read-only history. Never delete or modify archived folders; they
are the team's audit trail.